#pragma once
#include <string>

namespace GV{ //for global variables
  extern const std::string BUILD_DIR;
  extern const std::string TEST_DIR;
  extern const std::string SRC_DIR;
  extern const std::string PYTHON27_DIR; //Path to conda's Ptyhon2.7, with matplotlib installed
  extern bool        TEST_RUN;
}
