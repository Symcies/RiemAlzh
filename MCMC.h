#pragma once

#include <math.h>
#include "subject.h"

double proposition_distrib(double mean, double var);
/*
void simulation(std::vector<std::vector<double>>& z, std::vector<Subject> S);

std::vector<std::vector<double>> MCMC_SAEM(std::vector<Subject> S);
 */